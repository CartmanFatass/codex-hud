#!/usr/bin/env bash
# Open the detailed subagent tree page. Used by tmux display-popup.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
HUD_DIR="$(dirname "$SCRIPT_DIR")"
exec node "$HUD_DIR/dist/tree-page.js" "$@"
